---
import '../styles/global.css';
---

<!-- Cookiebot Consent Management Platform -->
<script
  id="Cookiebot"
  src="https://consent.cookiebot.com/uc.js"
  data-cbid="33fc1de1-c86c-46d0-9841-8c13574ee2af"
  data-blockingmode="none"
  type="text/javascript"
></script>

<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <title>Portfolio | Michał Pawlak</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&display=swap" rel="stylesheet" />

    <style>
      div#CybotCookiebotDialog {
        transform: scale(0.4);
        transform-origin: bottom right;
      }


      #about-button span {
        position: absolute;
        bottom: 0;
        left: 50%;
        height: 2px;
        width: 0;
        background: linear-gradient(to right, #22d3ee, #3b82f6, #06b6d4);
        transition: width 0.5s;
        transform: translateX(-50%);
      }

      #about-button:hover span {
        width: 100%;
      }

      @media (max-width: 768px) {
        #about-button {
          top: 1.5rem;
          right: auto;
          left: 50%;
          transform: translateX(-50%);
        }
      }

      .glow-line {
        filter: drop-shadow(0 0 8px #a78bfa);
      }
    </style>
  </head>

  <body class="relative font-mono text-black dark:text-lime-400 transition-colors duration-700 ease-in-out bg-white dark:bg-black overflow-x-hidden">

<!-- MENU z About -->
<div
  class="absolute top-0 right-0 z-10 bg-white/10 text-white
         flex items-start justify-start px-6 gap-6
         h-[5.4rem] w-[40vw] min-w-[280px] max-w-[520px] rounded-bl-3xl"
>
  <a
    href="#about"
    class="mt-4 text-white px-5 py-3 rounded-md font-semibold text-xl relative group"
  >
    About
    <span
      class="absolute bottom-0 left-1/2 h-[2px] w-0 bg-gradient-to-r from-cyan-400 via-blue-500 to-cyan-500 group-hover:w-full transition-all duration-500 transform -translate-x-1/2"
    ></span>
  </a>
</div>



    <div id="custom-cursor"></div>

    <header class="relative h-screen overflow-hidden flex flex-col items-center justify-center text-center px-4 sm:px-6">
      <video autoplay muted loop playsinline class="absolute top-0 left-0 w-full h-full object-cover -z-10">
        <source src="/video/layout4k.mp4" type="video/mp4" />
      </video>
      <div class="absolute inset-0 bg-black bg-opacity-40 -z-5"></div>

      <div class="z-10 animate-fadein leading-none select-none">
        <h1 id="dev" class="anton glitch-text text-white text-4xl sm:text-6xl md:text-[10vw]">
          DEVELOPER
        </h1>
        <div id="amp" class="glitch-text text-white text-3xl sm:text-5xl mt-1 mb-1">
          &
        </div>
        <h1 id="it" class="anton glitch-text text-white text-4xl sm:text-6xl md:text-[10vw]">
          IT TECHNICIAN
        </h1>
      </div>

      <div id="name-banner" class="fixed top-11 left-4 sm:left-12 z-50 select-none" style="transform: translateX(0px);">
        <h1 class="anton glitch-text text-white text-xl sm:text-5xl md:text-7xl">
          MICHAŁ PAWLAK
        </h1>
      </div>
    </header>

    <!-- SEKCJA ABOUT -->
    <section id="about" class="h-screen flex items-center bg-white dark:bg-black transition-colors relative">
      <div class="max-w-6xl mx-auto px-6 flex flex-col md:flex-row items-center gap-10">
        <!-- FOTO + BUTTONY -->
        <div class="flex flex-col items-center md:items-start w-full md:w-1/3">
          <div class="glitch-border w-80 h-80 rounded-full overflow-hidden relative" data-aos="fade-right" data-aos-delay="100">
            <img
              src="/img/michal.jpeg"
              alt="Michał Pawlak"
              class="w-full h-full object-cover rounded-full"
            />
          </div>

          <div class="flex gap-4 ml-6 mt-10" data-aos="fade-up" data-aos-delay="100">
            <a
              href="/files/michal-pawlak-resume.pdf"
              target="_blank"
              class="w-32 py-3 inter-font rounded glitch-border-button text-center text-black dark:text-cyan-400"
            >
              RESUME
            </a>
            <a
              href="https://www.linkedin.com/in/michał-pawlak-480297264/"
              target="_blank"
              class="w-32 py-3 inter-font rounded glitch-border-button text-center text-black dark:text-cyan-300"
            >
              LINKEDIN
            </a>
          </div>
        </div>

        <!-- OPIS -->
        <div class="w-full md:w-2/3 text-left" data-aos="fade-left" data-aos-delay="100">
          <h2 class="text-5xl font-bold mb-4 font-montserrat inter-font text-black-300 dark:text-cyan-300">Hi there!</h2>
          <ul class="text-xl leading-relaxed text-black dark:text-white inter-font list-inside space-y-3">
            <li>💻 Over 6 years creating games and apps for Android, iOS, and Windows (my true passion)</li>
            <li>🌐 4+ years IT experience, including modern website development</li>
            <li>⚡ 3 years as an electrician, plus 1 year independently designing and building EV charging stations</li>
            <li>🔧 Hobbyist programmer creating advanced ESP32 microcontroller projects</li>
            <li>🚀 Persistent and driven, always pushing to deliver outstanding results</li>
            <li>🚚 Licensed professional driver (C+E) with extensive experience in VIP transportation and freight logistics, showcasing exceptional responsibility, punctuality, and commitment</li>
          </ul>
        </div>

        <!-- PRZYCISK DALEJ -->
        <div class="absolute bottom-8 left-1/2 -translate-x-1/2">
          <a href="#projects" class="text-lime-400 hover:text-lime-300 font-semibold underline text-lg">
            ↓ Przejdź dalej
          </a>
        </div>
      </div>
    </section>

    <!-- SEKCJA PROJEKTY -->
    <section id="projects" data-aos="fade-left" class="py-16 sm:py-20 max-w-3xl mx-auto px-4 sm:px-6 transition-colors">
      <h2 class="text-3xl text-purple-400 mb-4 anton">🧰 Projekty</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div class="bg-lime-800 text-black p-4 rounded shadow">Portfolio z AOS i Astro</div>
        <div class="bg-lime-800 text-black p-4 rounded shadow">Aplikacja TODO w Tailwind</div>
      </div>
    </section>

    <!-- SEKCJA KONTAKT -->
    <section data-aos="zoom-in-up" class="py-16 sm:py-20 max-w-3xl mx-auto px-4 sm:px-6 text-center transition-colors">
      <h2 class="text-3xl text-pink-400 mb-4 anton">📞 Kontakt</h2>
      <p>
        Napisz do mnie na: <a href="mailto:michal.pawlak.praca@gmail.com" class="underline text-lime-300">michal.pawlak.praca@gmail.com</a>
      </p>
    </section>

    <footer class="text-center mt-10 py-6 px-4 text-sm text-white dark:text-gray-400 transition-colors font-[Montserrat] uppercase tracking-wide">
      MICHAŁ PAWLAK 2025 | ALL RIGHTS RESERVED ©
    </footer>

    <!-- Google Analytics przez Cookiebot -->
    <script type="text/plain" data-cookieconsent="statistics">
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-MN5XSVMVPC');
    </script>

    <!-- Skrypty -->
    <script src="https://unpkg.com/lottie-web@5.10.2/build/player/lottie.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js" onload="AOS.init({ once: false, disable: 'mobile' })"></script>
    <script is:inline src="/scripts/themeToggle.js"></script>
    <script is:inline src="/scripts/cursor.js"></script>

    <!-- Scroll Animation Script -->
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const banner = document.getElementById('name-banner');
        const dev = document.getElementById('dev');
        const amp = document.getElementById('amp');
        const it = document.getElementById('it');

        let targetOffset = 0;
        let currentOffset = 0;
        let devTarget = 0;
        let devCurrent = 0;
        let ampTarget = 0;
        let ampCurrent = 0;
        let itTarget = 0;
        let itCurrent = 0;

        const animate = () => {
          currentOffset += (targetOffset - currentOffset) * 0.1;
          banner.style.transform = `translateX(${-currentOffset}px)`;
          devCurrent += (devTarget - devCurrent) * 0.1;
          ampCurrent += (ampTarget - ampCurrent) * 0.1;
          itCurrent += (itTarget - itCurrent) * 0.1;

          dev.style.transform = `translateX(${devCurrent}px)`;
          amp.style.transform = `translateX(${-ampCurrent}px)`;
          it.style.transform = `translateX(${itCurrent}px)`;

          requestAnimationFrame(animate);
        };

        window.addEventListener('scroll', () => {
          const scrollY = window.scrollY;
          targetOffset = scrollY * 1.2;
          devTarget = scrollY * 1.0;
          ampTarget = scrollY * 1.2;
          itTarget = scrollY * 0.6;
        });

        animate();
      });
    </script>
  </body>
</html>
