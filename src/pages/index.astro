---
import '../styles/global.css';
---

<!-- Cookiebot Consent Management Platform -->
<script
  id="Cookiebot"
  src="https://consent.cookiebot.com/uc.js"
  data-cbid="33fc1de1-c86c-46d0-9841-8c13574ee2af"
  data-blockingmode="none"
  type="text/javascript"
></script>

<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <title>Portfolio | Michał Pawlak</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600&display=swap" rel="stylesheet" />

    <style>
      div#CybotCookiebotDialog {
        transform: scale(0.4);
        transform-origin: bottom right;
      }
    </style>
  </head>

  <body class="relative font-mono text-black dark:text-lime-400 transition-colors duration-700 ease-in-out bg-white dark:bg-black overflow-x-hidden">

    <div id="custom-cursor"></div>

    <header class="relative h-screen overflow-hidden flex flex-col items-center justify-center text-center px-4 sm:px-6">
      <video autoplay muted loop playsinline class="absolute top-0 left-0 w-full h-full object-cover -z-10">
        <source src="/video/layout4k.mp4" type="video/mp4" />
      </video>
      <div class="absolute inset-0 bg-black bg-opacity-40 -z-5"></div>

      <div class="z-10 animate-fadein leading-none select-none">
        <h1 id="dev" class="anton glitch-text text-white text-4xl sm:text-6xl md:text-[10vw]">
          DEVELOPER
        </h1>
        <div id="amp" class="glitch-text text-white text-3xl sm:text-5xl mt-1 mb-1">
          &
        </div>
        <h1 id="it" class="anton glitch-text text-white text-4xl sm:text-6xl md:text-[10vw]">
          IT TECHNICIAN
        </h1>
      </div>

      <div id="name-banner" class="fixed top-11 left-4 sm:left-12 z-50 select-none" style="transform: translateX(0px);">
        <h1 class="anton glitch-text text-white text-xl sm:text-5xl md:text-7xl">
          MICHAŁ PAWLAK
        </h1>
      </div>
    </header>

    <section data-aos="fade-right" class="py-16 sm:py-20 max-w-3xl mx-auto px-4 sm:px-6 transition-colors">
      <h2 class="text-3xl text-blue-400 mb-4 anton">👋 O mnie</h2>
      <p>
        Jestem początkującym twórcą frontendu. Lubię Tailwinda, Astro i AOS.
      </p>
    </section>

    <section data-aos="fade-left" class="py-16 sm:py-20 max-w-3xl mx-auto px-4 sm:px-6 transition-colors">
      <h2 class="text-3xl text-purple-400 mb-4 anton">🧰 Projekty</h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div class="bg-lime-800 text-black p-4 rounded shadow">Portfolio z AOS i Astro</div>
        <div class="bg-lime-800 text-black p-4 rounded shadow">Aplikacja TODO w Tailwind</div>
      </div>
    </section>

    <section data-aos="zoom-in-up" class="py-16 sm:py-20 max-w-3xl mx-auto px-4 sm:px-6 text-center transition-colors">
      <h2 class="text-3xl text-pink-400 mb-4 anton">📞 Kontakt</h2>
      <p>
        Napisz do mnie na: <a href="mailto:michal.pawlak.praca@gmail.com" class="underline text-lime-300">michal.pawlak.praca@gmail.com</a>
      </p>
    </section>

    <footer class="text-center mt-10 py-6 px-4 text-sm text-white dark:text-gray-400 transition-colors font-[Montserrat] uppercase tracking-wide">
      MICHAŁ PAWLAK 2025 | ALL RIGHTS RESERVED ©
    </footer>

    <!-- Google Analytics przez Cookiebot -->
    <script type="text/plain" data-cookieconsent="statistics">
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-MN5XSVMVPC');
    </script>

    <!-- Skrypty -->
    <script src="https://unpkg.com/lottie-web@5.10.2/build/player/lottie.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js" onload="AOS.init({ once: false, disable: 'mobile' })"></script>
    <script is:inline src="/scripts/themeToggle.js"></script>
    <script is:inline src="/scripts/cursor.js"></script>

    <!-- Scroll Animation Script -->
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const banner = document.getElementById('name-banner');
        const dev = document.getElementById('dev');
        const amp = document.getElementById('amp');
        const it = document.getElementById('it');

        let targetOffset = 0;
        let currentOffset = 0;
        let devTarget = 0;
        let devCurrent = 0;
        let ampTarget = 0;
        let ampCurrent = 0;
        let itTarget = 0;
        let itCurrent = 0;

        const animate = () => {
          currentOffset += (targetOffset - currentOffset) * 0.1;
          banner.style.transform = `translateX(${-currentOffset}px)`;
          devCurrent += (devTarget - devCurrent) * 0.1;
          ampCurrent += (ampTarget - ampCurrent) * 0.1;
          itCurrent += (itTarget - itCurrent) * 0.1;

          dev.style.transform = `translateX(${devCurrent}px)`;
          amp.style.transform = `translateX(${-ampCurrent}px)`;
          it.style.transform = `translateX(${itCurrent}px)`;

          requestAnimationFrame(animate);
        };

        window.addEventListener('scroll', () => {
          const scrollY = window.scrollY;
          targetOffset = scrollY * 1.2;
          devTarget = scrollY * 1.0;
          ampTarget = scrollY * 1.2;
          itTarget = scrollY * 0.6;
        });

        animate();
      });
    </script>
  </body>
</html>
